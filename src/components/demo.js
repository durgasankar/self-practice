import React from 'react'

// we can export by this and use that
export const Welcome = () => { return <h1>r20jc134</h1> }

const Greet = () => { return <h1>Hey Welcome... Nikl phle fursat me.</h1> }

// we can also export by this and only one defalut exports remailns 
export default Greet;